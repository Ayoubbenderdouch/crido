import 'package:flutter/material.dart';

/// Crido brand palette — mirrors the web/dashboard design system.
class AppColors {
  AppColors._();

  // Brand — Crido teal
  static const teal = Color(0xFF0F6E56);
  static const tealDeep = Color(0xFF04342C);
  static const tealNight = Color(0xFF06241E);
  static const tealHover = Color(0xFF0C5B47);
  static const tealBright = Color(0xFF1D9E75);
  static const tealSurface = Color(0xFFE1F5EE);

  // Warm canvas
  static const white = Color(0xFFFFFFFF);
  static const cream = Color(0xFFF7F6F1);
  static const creamDeep = Color(0xFFEEEDE3);

  // Ink
  static const ink = Color(0xFF1A1A18);
  static const inkSoft = Color(0xFF56554F);
  static const inkFaint = Color(0xFF8B8A83);
  static const line = Color(0xFFE6E4DA);

  // Semantic
  static const success = Color(0xFF1D9E75);
  static const warning = Color(0xFFEF9F27);
  static const danger = Color(0xFFE24B4A);
  static const info = Color(0xFF378ADD);
  static const amber = Color(0xFFEF9F27);
}

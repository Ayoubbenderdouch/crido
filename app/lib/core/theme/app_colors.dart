import 'package:flutter/material.dart';

/// Crido brand palette — see docs/DESIGN_SYSTEM.md
class AppColors {
  AppColors._();

  // Brand — Crido Teal
  static const cridoTeal = Color(0xFF0F6E56);
  static const cridoTealHover = Color(0xFF0C5B47);
  static const cridoTealLight = Color(0xFF1D9E75);
  static const cridoTealSurface = Color(0xFFE1F5EE);

  // Semantic
  static const success = Color(0xFF1D9E75);
  static const warning = Color(0xFFEF9F27);
  static const danger = Color(0xFFE24B4A);
  static const info = Color(0xFF378ADD);

  // Neutrals — light mode
  static const background = Color(0xFFFFFFFF);
  static const backgroundSecondary = Color(0xFFF7F6F1);
  static const foreground = Color(0xFF1A1A18);
  static const foregroundSecondary = Color(0xFF5F5E5A);
  static const foregroundTertiary = Color(0xFF888780);
}
